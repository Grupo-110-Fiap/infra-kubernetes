apiVersion: v1
kind: Service
metadata:
  name: pedidos-api
  namespace: pedidos
spec:
  type: LoadBalancer
  selector:
    app: pedidos-api
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP